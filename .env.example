# Copy these values into your deployment provider's environment settings.
# Do not commit a real password here.

NODE_ENV=production
HOST=0.0.0.0
PORT=8000
ADMIN_PASSWORD=change-this-before-deploying

# Optional: use this when your host gives you a persistent disk/folder.
# DATA_DIR=/var/data/restaurant-website
